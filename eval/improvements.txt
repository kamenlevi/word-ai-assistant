**L1-paragraph-format-001**
```javascript
await applyStyle(doc.body.paragraphs[0], "Heading 1");
await applyStyle(doc.body.paragraphs[0], { fontSize: 14 });
```

**L1-list-bullet-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.addList(["Apples", "Bananas", "Cherries"], "bullet");
  await context.sync();
});
```

**L1-list-multilevel-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.addList(["Plan", "Execute", "Review"], "number", { level: 0 });
  await context.sync();
});
```

**L1-find-replace-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.replaceText("Acme", "Globex");
  await context.sync();
});
```

**L1-find-replace-regex-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.replaceText(".*?@.*?\\..*", "[redacted]", { matchWildcards: true });
  await context.sync();
});
```

**L1-footnote-insert-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertFootnote("See appendix A");
  await context.sync();
});
```

**L1-comment-insert-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertComment("Revenue reached $4.2M last quarter.", "check this number");
  await context.sync();
});
```

**L1-section-break-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertSectionBreak("continuous");
  await context.document.body.sections.getItem(context.document.body.sections.length - 1).setPageOrientation("landscape");
  await context.sync();
});
```

**L1-header-footer-002**
```javascript
await Word.run(async (context) => {
  await context.document.body.addPageNumbers({ location: "footer", alignment: "right", format: "i" });
  await context.sync();
});
```

**L1-columns-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertSectionBreak("continuous");
  await context.document.body.sections.getItem(context.document.body.sections.length - 1).setColumns
