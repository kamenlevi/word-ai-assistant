**L1-list-bullet-001**
```javascript
await addParagraph("Apples, Bananas, Cherries");
await context.sync();
```

**L1-list-multilevel-001**
```javascript
await addParagraph("Plan");
await addList(["Execute", "Review"], { level: 1 });
await context.sync();
```

**L1-table-create-001**
```javascript
await insertTable(4, 3, [["Quarter", "Revenue", "Notes"], ["Q1", 100, ""], ["Q2", 120, ""], ["Q3", 140, ""]], { headerRow: true, style: "Grid Table 4 - Accent 1" });
await context.sync();
```

**L1-table-format-001**
```javascript
const table = doc.tables.getFirst();
await applyStyle(table, "Grid Table 4 - Accent 1");
await context.sync();
```

**L1-find-replace-001**
```javascript
await replaceText("Acme", "Globex");
await context.sync();
```

**L1-find-replace-002**
```javascript
const results = doc.body.search("WARNING", { matchCase: true });
results.load("font");
results.items.forEach(r => { r.font.bold = true; r.font.color = "#D32F2F"; });
await context.sync();
```

**L1-find-replace-regex-001**
```javascript
await replaceText("<*@*.*>", "[redacted]", { matchWildcards: true });
await context.sync();
```

**L1-footnote-insert-001**
```javascript
await insertFootnote("See appendix A.", { location: "current selection" });
await context.sync();
```

**L1-image-insert-001**
```javascript
await insertImage("office-workplace", ["hospital", "medical"], { width: 320 });
await context.sync();
```

**L1-image-insert-002**
```javascript
await insertImage("healthcare", ["hospital", "medical"], { width: 360 });
await context.sync();
```

**L1-watermark-insert-001**
```javascript
await insertWatermark("CONFIDENTIAL");
await context.sync();
```

**L1-content-control-001**
```javascript
await insertContentControl("datePicker", { title: "Sign Date", tag: "signDate" });
await context.sync();
```

**L1-content-control-002**
```javascript
await insertContentControl
