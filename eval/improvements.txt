Here are the new rules and examples to add to the system prompt:

**L1-heading-insert-001**
```javascript
await addHeading("Quarterly Review", 1);
await context.sync(); // Ensure context is updated
```

**L1-heading-insert-002**
```javascript
await addHeading("Methodology", 2);
await addParagraph("We used a randomized controlled trial design.");
await context.sync(); // Ensure context is updated
```

**L1-paragraph-format-001**
```javascript
const paragraph = doc.paragraphs.getFirst();
paragraph.font.bold = true;
paragraph.font.size = 14;
await context.sync();
```

**L1-list-bullet-001**
```javascript
const paragraph = doc.paragraphs.add();
paragraph.listItem = true;
paragraph.listType = "Bullet";
paragraph.text = "Apples";
await context.sync();
```

**L1-find-replace-001**
```javascript
await replaceText("Acme", "Globex", { matchCase: true });
await context.sync(); // Ensure context is updated
```

**L1-find-replace-002**
```javascript
const results = doc.body.search("WARNING", { matchCase: true });
results.load("font");
results.items.forEach(r => { r.font.bold = true; r.font.color = "#D32F2F"; });
await context.sync();
```

**L1-find-replace-regex-001**
```javascript
await replaceText("<*@*.*>", "[redacted]", { matchWildcards: true });
await context.sync(); // Ensure context is updated
```

**L1-footnote-insert-001**
```javascript
await insertFootnote("See appendix A.", { location: "current selection" });
await context.sync(); // Ensure context is updated
```

**L1-comment-insert-001**
```javascript
const paragraph = doc.paragraphs.getFirst();
const comment = paragraph.comments.add();
comment.text = "check this number";
await context.sync();
```

**L1-track-changes-toggle-001**
```javascript
await toggleTrackChanges(true);
await context.sync(); // Ensure context is updated
```

**L1-section-break-001**
```javascript
await insertSectionBreak("continuous");
await setPageOrientation("landscape");
await context.sync(); // Ensure context is updated
```

**L1-header-footer-001**
```javascript
const section = doc.sections.getFirst();
const header = section.header.getFirst();
const paragraph = header.paragraphs.add();
