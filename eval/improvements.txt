**L1-heading-insert-001**
```javascript
await addHeading("Quarterly Review", 1);
await context.sync();
```

**L1-paragraph-format-001**
```javascript
await applyStyle(doc.body.paragraphs[0], "Normal");
await context.sync();
await doc.body.paragraphs[0].font.size = 14;
await context.sync();
```

**L1-list-bullet-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.addParagraph("Apples, Bananas, Cherries");
  await context.sync();
  await context.document.body.addList(["Apples", "Bananas", "Cherries"], "bullet");
  await context.sync();
});
```

**L1-find-replace-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.replaceText("Acme", "Globex", { matchWildcards: true });
  await context.sync();
});
```

**L1-comment-insert-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.findText("$4.2M", { matchWildcards: true });
  await context.document.body.insertComment(context.document.body.findText("$4.2M", { matchWildcards: true }).items[0].paragraph, "check this number");
  await context.sync();
});
```

**L1-section-break-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertSectionBreak("continuous");
  await context.document.body.setPageOrientation("landscape");
  await context.sync();
});
```

**L1-columns-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertSectionBreak("continuous");
  await context.document.body.insertColumns(2);
  await context.sync();
});
```

**L1-image-insert-001**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertImage("office-workplace", ["desk","minimal"], { width: 320 });
  await context.sync();
});
```

**L1-image-insert-002**
```javascript
await Word.run(async (context) => {
  await context.document.body.insertImage("healthcare", ["hospital","medical"], { width: 360 });
  await context.sync();
});
```

**L1-watermark-insert-001**
```javascript
await Word.run(async (context) => {
  await context
